{
  "name": "gh-workflows",
  "version": "0.1.0",
  "description": "GitHub Actions workflows package",
  "license": "MIT",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "postinstall": "epic-postinstall",
    "lint": "actionlint",
    "clean": "epic-postinstall --uninstall && rimraf node_modules .turbo"
  },
  "dependencies": {},
  "devDependencies": {
    "@repo/epic-postinstall": "workspace:*",
    "rimraf": "^6.0.1"
  }
}