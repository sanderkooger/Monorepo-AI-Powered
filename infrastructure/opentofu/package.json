{
  "name": "opentofu",
  "scripts": {
    "lint:infra": "pnpm npm-run-all lint:tflint",
    "lint:tflint": "tflint --recursive --color",
    "lint:checkov": "checkov --directory . --config-file .checkov.yaml"
  },

  "dependencies": {
    
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^6.0.1"
  }
}