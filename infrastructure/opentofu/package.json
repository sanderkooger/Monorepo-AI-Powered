{
  "name": "opentofu",
  "scripts": {
    "lint:infra": "npm-run-all lint:checkov lint:tflint",
    "lint:tflint": "tflint --recursive --config .tflint.hcl",
    "lint:checkov": "checkov --directory . --config-file .checkov.yaml"
  },
  "devDependencies": {
    "tflint": "^0.51.0",
    "checkov": "^3.1.42",
    "npm-run-all": "^4.1.5"
  }
}