{
  "name": "@monorepo/ansible",
  "version": "0.1.0",
  "description": "Ansible environment management package",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "postinstall": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
    "lint:off": "ansible-lint",
    "clean": "rimraf .venv node_modules"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "private": true,
  "dependencies": {
     
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}