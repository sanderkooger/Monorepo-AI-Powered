import { type Config } from "prettier";

const defaultConfig: Config = {
  printWidth: 80,
  trailingComma: "none",
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  arrowParens: "always",
};

export { defaultConfig };